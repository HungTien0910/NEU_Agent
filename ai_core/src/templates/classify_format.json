{
  "type": "json_schema",
  "json_schema": {
    "name": "question_mode",
    "strict": true,
    "schema": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mode": { "type": "string", "enum": ["data", "chat"] },
        "reason": { "type": "string" }
      },
      "required": ["mode", "reason"]
    }
  }
}
